name = "morgann-music-cp"
type = "javascript"
pages_build_output_dir = ""
compatibility_date = "2024-01-01"

# Environment variables for Brevo integration
env.production.vars = { BREVO_FROM_EMAIL = "no-reply@mm-cp.uk", BREVO_FROM_NAME = "Morgann Music CP" }

# Sensitive secrets (set via Cloudflare Dashboard or CLI)
# wrangler secret put BREVO_API_KEY --env production
